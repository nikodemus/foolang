import .prefixedImport.*

class Main {}
    direct method run: cmd in: sys
        let out = sys output.
        out println: "X = {X}".
        out println: "Y = {Y value}".
        out println: "_Y = {_Y}".
        sys exit!
end
