import .impl.formatter.Formatter

class Main {}
    direct method run: files in: system
        files
            do: { |file|
                  Formatter
                      formatInPlace: system files / file
                      reportingTo: system output }!
end
