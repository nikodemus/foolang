import .impl.compiler

class Main {}
    direct method run: command in: system
        -- KLUDGE: prefix to avoid colliding with bootstrap evaluator
        -- class called compiler.
        compiler.Compiler compile: command first
                          in: system
                          as: command second!
end
