define DirectMethods
    Dictionary new!

define InstanceMethods {
    #clock
    -> { signature: [], vars: 0,
         body: "return INSTANCE(Clock, NULL);" },
    #files
    -> { signature: [], vars: 0,
         body: "struct FooBytes* root = FooBytes_from(\"\");
                return INSTANCE(FilePath, root);" },
    #currentDirectory
    -> { signature: [], vars: 0,
         body: "struct FooBytes* root = FooBytes_from(\"./\");
                return INSTANCE(FilePath, root);" },
    #gc
    -> { signature: [], vars: 0,
         body: "foo_gc(ctx);
                return foo_Boolean_new(true);" },
    #gcTrace
    -> { signature: [], vars: 0,
         body: "bool old_trace = trace_gc;
                bool old_verbose = gc_verbose;
                trace_gc = true;
                gc_verbose = true;
                foo_gc(ctx);
                gc_verbose = old_verbose;
                trace_gc = old_trace;
                return foo_Boolean_new(true);" },
    #gcVerbose
    -> { signature: [], vars: 0,
         body: "bool old_verbose = gc_verbose;
                gc_verbose = true;
                foo_gc(ctx);
                gc_verbose = old_verbose;
                return foo_Boolean_new(true);" },
    #output
    -> { signature: [], vars: 0,
         body: "return INSTANCE(Output, stdout);" },
    #random
    -> { signature: [], vars: 0,
         body: "return INSTANCE(SystemRandom, NULL);" },
}!
